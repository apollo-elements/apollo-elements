schema: null

extensions:
  codegen:
    config:
      constEnums: true
      declarationKind: interface
      dedupeOperationSuffix: true
      immutableTypes: true
      namingConvention: keep
      noSchemaStitching: true
      operationResultSuffix: Data
      documentVariableSuffix: ''
      optionalResolveType: true
      useIndexSignature: true

    generates:
      test/test/schema.ts:
        schema: test/test/graphql/test.schema.graphql
        plugins:
          - typescript
          - typescript-operations
          - typed-document-node

        documents:
          - test/test/graphql/*.query.graphql
          - test/test/graphql/*.mutation.graphql
          - test/test/graphql/*.subscription.graphql
          - test/test/graphql/*.fragment.graphql
