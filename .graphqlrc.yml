schema: null

extensions:
  codegen:
    config:
      constEnums: true
      declarationKind: interface
      dedupeOperationSuffix: true
      immutableTypes: true
      namingConvention: keep
      noSchemaStitching: true
      operationResultSuffix: Data
      documentVariableSuffix: ''
      optionalResolveType: true
      useIndexSignature: true

    generates:
      private/test/schema.ts:
        schema: private/test/graphql/test.schema.graphql
        plugins:
          - typescript
          - typescript-operations
          - typed-document-node

        documents:
          - private/test/graphql/*.query.graphql
          - private/test/graphql/*.mutation.graphql
          - private/test/graphql/*.subscription.graphql
          - private/test/graphql/*.fragment.graphql
