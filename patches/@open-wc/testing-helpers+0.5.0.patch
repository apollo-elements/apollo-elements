patch-package
--- a/node_modules/@open-wc/testing-helpers/helpers.js
+++ b/node_modules/@open-wc/testing-helpers/helpers.js
@@ -1,5 +1,3 @@
-let defineCECounter = 0;
-
 /**
  * Registers a new element with an automatically generated unique name.
  * Helps to make a test fully isolated.
@@ -15,9 +13,8 @@ let defineCECounter = 0;
  * @returns {string}
  */
 export function defineCE(klass) {
-  const tag = `test-${defineCECounter}`;
+  const tag = `test-${Date.now()}`;
   customElements.define(tag, klass);
-  defineCECounter += 1;
   return tag;
 }
 
