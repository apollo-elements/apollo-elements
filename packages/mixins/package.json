{
  "name": "@apollo-elements/mixins",
  "version": "2.0.1",
  "description": "👩‍🚀🌛 Custom Element class mixins for Apollo GraphQL 🚀👨‍🚀",
  "main": "index.js",
  "module": "index.js",
  "scripts": {
    "prepublishOnly": "tsc -b .",
    "build": "tsc",
    "clean": "run-s clean:*",
    "clean:js": "rm -f ./*.js",
    "clean:dts": "rm -f ./*.d.ts;",
    "clean:map": "rm -f ./*.js.map",
    "start": "tsc -w --noEmit",
    "docs": "run-s docs:*",
    "docs:polymer": "polymer analyze > docs/analysis.json",
    "docs:wca": "wca analyze --outFile ./custom-elements.json",
    "test": "wtr --coverage --root-dir ../.."
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.com"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/apollo-elements/apollo-elements.git"
  },
  "keywords": [
    "Apollo",
    "GraphQL",
    "Web Components",
    "Custom Elements"
  ],
  "author": "Benny Powers <web@bennypowers.com>",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/apollo-elements/apollo-elements/issues"
  },
  "homepage": "https://github.com/apollo-elements/apollo-elements#readme",
  "dependencies": {
    "@apollo-elements/interfaces": "^1.0.0-alpha.0",
    "@apollo-elements/lib": "^3.0.0-alpha.0",
    "@apollo/client": ">= 3",
    "@apollo-elements/lib": "^2.0.0",
    "@open-wc/dedupe-mixin": "^1.3.0",
    "bind-decorator": "^1.0.11",
    "tslib": "^2.0.1"
  }
}
